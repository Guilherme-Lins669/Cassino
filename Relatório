Relatório de Desenvolvimento do Projeto

=-Dia 18/09/2025-=
Início do projeto. Havia dúvida entre desenvolver um projeto mais complexo utilizando Python Flask ou criar um jogo de azar pelo 
terminal do compilador. Após análise do desenvolvedor responsável, foi decidido desenvolver o programa utilizando Python Flask, 
com o objetivo de criar um sistema mais completo.

=-Dia 19/09/2025-=
Início da programação do projeto. Foram encontrados diversos erros de sintaxe e problemas de conectividade. 
Com o tempo e a realização de pesquisas apropriadas, a maioria dos erros foi solucionada.

=-Dia 21/09/2025-=
Primeira versão do projeto concluída. O HTML, CSS e JavaScript foram implementados de forma básica,
apenas para testar o funcionamento do sistema. Houve problemas iniciais com o banco de dados e o redirecionamento das páginas.

=-Dia 22/09/2025-=
Correções realizadas no redirecionamento das páginas. Banco de dados ajustado, possibilitando o armazenamento do histórico,
nome e saldo do usuário. Iniciou-se a melhoria do layout visual do site.

=-Dia 24/09/2025-=
Site praticamente concluído (99%), restando apenas algumas alterações básicas e ajustes simples no design visual.

=-Dia 27/09/2025-=
Site concluído, sem necessidade de alterações adicionais.

=-EXPLICANDO OS ARQUIVOS-=

=-BASE.HTML-=
funciona como o esqueleto principal da aplicação web
Ele define a estrutura comum que será usada por
todas as páginas do site, garantindo que elementos como o
cabeçalho, links de CSS, scripts JavaScript e configurações de responsividade sejam 
carregados automaticamente, sem a necessidade de repetir esse código em cada página individual.

=-DEPOSIT.HTML-=
Este arquivo é um template de página específico da aplicação, utilizado para
exibir a interface de depósito do jogador.
Ele herda a estrutura principal do base.html e preenche o bloco de conteúdo definido ali, mantendo o layout e
os recursos padrão do site (como estilos e scripts)
Seu objetivo é permitir que um jogador logado informe um valor em reais (R$) para depositar em sua conta virtual. 
A página exibe uma saudação com o nome do jogador e.
caso haja algum erro (como uma tentativa inválida de depósito), também mostra uma mensagem de erro.

=-GAME_DICE.HTML-=
O arquivo de Dados é a interface frontend que permite ao usuário interagir com a lógica do jogo de dados.

Ele é o responsável por:

Exibir o saldo e as regras de pagamento.

Capturar a aposta e o palpite do jogador.

Comunicar esses dados ao backend (via main.js).

Exibir o resultado da jogada de forma dinâmica.

=-GAME_MINESWEEPER.HTML-=
Este arquivo é a página do jogo Campo Minado dentro do cassino. Ele mostra a tela onde o 
jogador faz uma aposta e começa a jogar, clicando em células de uma grade 5x5.
Além disso, o arquivo já tem um pouco de JavaScript embutido, que cuida da parte interativa:

Envia a aposta para o servidor.

Monta o tabuleiro na tela.

Lida com os cliques do jogador.

Mostra se ele ganhou ou perdeu.

Atualiza o saldo automaticamente.

É a interface completa do jogo Campo Minado, feita para funcionar direto no navegador, 
se comunicando com o servidor via requisições (fetch)

=-GAME_ROCKET.HTML-=
Este arquivo é a página do jogo do Foguete no cassino.
Ele mostra uma interface onde o jogador pode:
Ver seu saldo atual Informar um valor de aposta.
Escolher se acha que o foguete vai subir ou descer.
Clicar em "Lançar" para fazer a aposta.
Depois disso, o resultado do jogo aparece na tela (se o jogador ganhou ou perdeu).
Usa um multiplicador fixo de 0.25x (provavelmente para calcular ganhos).

Carrega o estilo do site (com style.css).

Carrega o JavaScript (main.js) que cuida da lógica e interações.

É uma tela simples, feita para o jogador apostar rapidamente e voltar para o lobby se quiser

=-GAME_ROULETTE.HTML-=
O arquivo game_roulette.html é um template HTML é a página que permite ao jogador apostar em uma cor 
e tentar a sorte girando a roleta. Ele combina visual, interação e comunicação com o servidor para 
entregar uma experiência de jogo rápida e direta. Toda a lógica de validação, sorteio da cor vencedora e 
atualização de saldo é feita no backend (Python/Flask) e retornada para essa página por meio do JavaScript.

=-GAME_SLOTS.HTML-=
 O arquivo game_slots.html é o template da página do jogo de Caça-Níqueis (ou Slots) do cassino virtual. Ele é responsável
por mostrar ao jogador a interface onde ele pode apostar,
girar os rolos e ver o resultado do jogo em tempo real.
O template é escrito em HTML com o motor de templates Jinja2, que permite inserir dados dinâmicos vindos do backend Flask,
como o saldo do jogador.
O CSS é carregado a partir do arquivo style.css para manter a aparência visual consistente com o restante do site.

A página também contém JavaScript integrado, que:

Captura o valor da aposta.

Envia a aposta para o servidor (backend) usando fetch() via POST na rota /play/caca_niqueis.

Recebe a resposta com o resultado (os números sorteados e o saldo atualizado).

Atualiza dinamicamente a tela com os rolos e o saldo, sem precisar recarregar a página.

O design dos rolos é definido com classes CSS (.reels e .reel) para dar o estilo de "máquina caça-níqueis"

=-HISTORY.HTML-=
O arquivo historico.html é um template HTML que exibe o histórico de partidas do jogador no sistema de cassino online. 
Ele mostra de forma organizada todas as apostas feitas pelo usuário, incluindo os jogos que ele jogou, os valores apostados, 
os ganhos ou perdas, e o saldo final após cada jogada tambem Exibe o nome do jogador
O título da página mostra “Histórico de [nome do jogador]”, deixando claro que os dados exibidos pertencem ao usuário logado.

=-LOBBY.HTML-=
O lobby.html é a tela principal do cassino, onde o jogador escolhe um jogo para jogar, 
vê seu saldo, acessa seu histórico e faz depósitos. É uma interface organizada, 
interativa e eficiente para centralizar a navegação dentro do sistema de minigames.
O jogador pode digitar o valor a ser depositado e enviar diretamente, sem sair da página.
Ao lado, o saldo do jogador aparece em reais, formatado com duas casas decimais.

Apresenta os links para todos os jogos disponíveis

Cada jogo é representado por um link com um ícone e o nome:

Foguete 🚀

Campo Minado 💣

Caça Níqueis 🎰

Roleta 🎡

Dados 🎲

=-LOGIN.HTML-=
O arquivo login.html é o template responsável por exibir a tela de login do sistema de cassino. Ele serve como
ponto de entrada principal da aplicação, permitindo que o usuário se identifique antes de acessar os jogos.
Quando o usuário insere um nome e clica em "Entrar", o servidor Python, que atende a rota /login, é responsável por:
Verificar o nome fornecido. Se o nome existir no banco de dados (cassino.db na tabela player), ele carrega a sessão do usuário
Se o nome não existir, ele cria um novo jogador no banco de dados.
Após a autenticação ou criação, o servidor geralmente redireciona o usuário para o Lobby de Minigames

=-APP.PY-=
app.py, é o núcleo do backend da aplicação de cassino desenvolvida com o framework Flask.
Ele é responsável por orquestrar toda a lógica do sistema, é o ponto central do sistema, unificando a lógica de jogos, 
controle de usuários, banco de dados e interface com o frontend.

Sem ele, o projeto não conseguiria:

Iniciar sessões de usuários.

Gerenciar apostas e calcular resultados.

Registrar partidas no histórico.

Proteger rotas privadas (como o lobby).

Sincronizar os dados entre servidor e interface.

=-EXTENSION.PY-=
Este arquivo define e exporta a instância principal do SQLAlchemy, 
a ferramenta usada para conectar o Flask com o banco de dados (neste caso, SQLite).
Ele inicializa o objeto db, que representa a conexão e os recursos do SQLAlchemy.
Esse objeto será usado para Criar as tabelas (modelos).
Fazer queries no banco de dados.
Realizar inserções, atualizações e exclusões.

=-MODELS.PY-=
Este arquivo define os modelos de dados do  cassino, ou seja.
as tabelas que serão criadas no banco de dados cassino.db com ajuda do SQLAlchemy (ORM do Flask).
Esses modelos são usados para criar o banco de dados (com db.create_all()).
Salvar dados persistentes das apostas consultar histórico, saldo, e estatísticas do jogador.
Validar regras do jogo (pode apostar? saldo suficiente? etc

=-STYLE.CSS-=
Este CSS cria uma interface moderna no estilo “dark mode”, com foco em:
Legibilidade (textos claros em fundo escuro).
Visual DE (cores douradas e cinzas).
FaciliTANDO de navegação (botões grandes e responsivos).
Clareza em tabelas (bom para histórico de jogos).

=-MAIN.JS-=
Este arquivo, main.js, é o cérebro da interatividade (frontend) do seu código de cassino.
Ele é o responsável por fazer a aplicação parecer um site moderno e fluido, utilizando AJAX (Asynchronous JavaScript and XML) para 
se comunicar com o servidor Python (app.py) sem que a página precise recarregar.
Deixa a interface dinâmica e moderna
Substitui recarregamentos de página por requisições AJAX
Garante interatividade sem travamentos
Cuida de fluxos críticos como: jogar, depositar, atualizar saldo e navegar pelo lobby

=-EXPLICANDO O CODIGO DO ARQUIVO-=

---
## BASE.HTML

## CODIGO HTML

*<!doctype html>	Estrutura	Define o tipo de documento como HTML5.

<html lang="pt-br">	O elemento raiz de todo o documento, definindo o idioma como Português Brasileiro.

<head>		Contém informações sobre o documento que não são exibidas na página (como título, codificação e links para CSS).

<meta charset="utf-8">		Define a codificação de caracteres para suportar a maioria dos idiomas.

<meta name="viewport" ...>	Metadados	Configura a forma como a página deve ser exibida em diferentes dispositivos (essencial para responsividade).
<title>CASSINO</title>		Define o texto que aparece na aba do navegador.

<link rel="stylesheet" ...>	Linka o arquivo de estilo CSS (style.css) ao documento.

<body>		Contém o conteúdo visível da página.

<div>		Um contêiner genérico (id="app") usado para agrupar outros elementos HTML.

<script>	Conteúdo	Usado para incorporar ou referenciar código JavaScript.

## DEPOSIT.HTML


{% extends 'base.html' %}
Indica que esta página herda o layout principal definido em base.html.

{% block content %}
Início do bloco onde será colocado o conteúdo específico desta página.

<main class="center">  
Define a área principal da página e aplica a classe "center" para centralizar o conteúdo.  

<h2>Bem-vindo, {{ player.name }}</h2>  
Exibe uma saudação personalizada com o nome do jogador logado.  

{% if error %}
Verifica se existe alguma mensagem de erro a ser exibida.

<p class="error">{{ error }}</p>  
Mostra a mensagem de erro (se existir), estilizada com a classe "error".  

{% endif %}
Fecha a estrutura condicional de exibição do erro.

<form method="post" action="{{ url_for('deposit') }}">  
Cria um formulário que envia dados via POST para a rota "deposit".  

<label>Quanto deseja depositar (R$)?</label>
Exibe um texto informando o que o usuário deve preencher.

<input name="amount" type="number" step="0.01" min="0.01" required>  
Campo para digitar o valor do depósito; só aceita números positivos com duas casas decimais.  

<button type="submit">Depositar</button>
Botão que envia o formulário.

</form> Marca o fim do formulário.  
<a href="{{ url_for('index') }}">Cria um link que redireciona o usuário para a página inicial.  
<button type="button" class="btn btn-primary">⬅ Voltar</button>
Botão estilizado que permite voltar para a página inicial
</a> Fecha o link da navegação.  
</main> Fecha a seção principal da página.  

{% endblock %}
Finaliza o bloco de conteúdo iniciado anteriormente.

### GAME_DICE.HTML

<!DOCTYPE html>	Definição de Tipo	Define o documento como um HTML5.
<html lang="pt-br">		O contêiner principal do documento, definindo a linguagem como Português do Brasil.
<head>	Contém informações sobre o documento que não são exibidas na página.
<meta charset="UTF-8">		Garante que acentos e caracteres especiais sejam exibidos corretamente.
<title>	Título da Página	Define o título que aparece na aba ou janela do navegador.
<link>	Estilo (CSS)	Liga o arquivo de estilo externo (style.css) para definir a aparência visual da página.
<script defer>	Liga o arquivo JavaScript principal (main.js). O atributo defer garante que o script seja executado após todo o HTML ser carregado, sendo essencial para a lógica de AJAX.

<body>	Conteúdo Visível	Contém todo o conteúdo que o usuário vê.
<div class="container">	Layout	Um contêiner de propósito geral, usado para aplicar estilos de layout (como centralização ou largura máxima) definidos no style.css.
<h1>	Título Principal	Exibe o título "🎲 Dados".
<p>	Parágrafo	Usado para exibir as regras (Multiplicador) e informações de conta (Saldo).
<span id="saldo">	Saldo Dinâmico	Um contêiner inline. O id="saldo" é crucial, pois o JavaScript (main.js) usa esse ID para atualizar o saldo em tempo real após cada aposta. O código `{{ '%.2

form id="dados-form">	Formulário de Aposta	Agrupa os campos de aposta. O id="dados-form" é o alvo do JavaScript para interceptar a submissão e enviar a jogada por AJAX à rota /play/dados.
<input type="number" name="bet" ...>	Valor da Aposta	Campo para o usuário digitar o valor em dinheiro. O name="bet" é o nome da variável enviada ao servidor.
<label for="guess">	Rótulo	Define o texto descritivo para o campo de palpite, associado ao input com id="guess".
<input id="guess" type="number" name="guess" ...>	Palpite da Soma	Campo para o usuário adivinhar a soma dos dados. Os atributos min="5" e max="30" restringem o valor aos limites lógicos do jogo.
<button type="submit">	Botão de Ação	Inicia a submissão do formulário, que é então processada pelo JavaScript.

<div id="resultado">	Mensagens do Jogo	Uma área vazia onde o JavaScript irá injetar dinamicamente o resultado da jogada (os números sorteados, se ganhou ou perdeu, e o prêmio).
<a>	Hyperlink	Permite ao usuário retornar ao Lobby do cassino. O {{ url_for('lobby') }} é uma função do Jinja2 que gera o link correto para a rota de lobby do Flask.

 ### GAME_MINESWEEPER.HTML

<style>	Estilo do Jogo	Define o visual específico do Campo Minado. O .grid usa display: grid e grid-template-columns: repeat(5, 50px) para criar um tabuleiro perfeito de 5×5. O .cell define o tamanho e a aparência dos quadrados, e as classes .revealed e .bomb controlam as cores e cursores após o clique.
<div class="container">	Layout	Contêiner principal para centralizar o conteúdo e aplicar o estilo geral do site.
<p> Saldo: R$ <span id="saldo">...</span> </p>	Saldo Dinâmico	Exibe o saldo. O id="saldo" é usado pelo JavaScript para atualizar o valor após cada clique seguro ou a perda do jogo.

<form id="start-form">	Início da Aposta	Este formulário não usa o main.js genérico, mas sim o script dedicado abaixo. Ele é usado apenas para coletar a aposta inicial.
<input name="bet" ...>	Valor da Aposta	Campo onde o jogador insere o valor a ser apostado para esta rodada de Campo Minado.
<button type="submit">	Inicia Jogo	Aciona o evento que chama a rota /minesweeper/start no backend via AJAX.
<div id="game">	Tabuleito	É o contêiner vazio onde o JavaScript desenha o tabuleiro 5×5 quando o jogo é iniciado.
<div id="resultado">	Mensagens	Área para exibir mensagens de status (erros de aposta, "Jogo iniciado", "Célula segura!" ou "Você perdeu!").

### GAME_ROCKET.HTML

<h1>Foguete</h1>	Título Principal	O nome do jogo exibido ao usuário.
<p>Multiplicador: 0.25x</p>	Regra do Jogo	Informa ao jogador que, se ele acertar, o ganho será de 0.25 vezes o valor apostado. Este é um multiplicador baixo, típico de jogos com alta chance de acerto (50% no caso up/down).
<span id="saldo">...</span>	Saldo Dinâmico	Exibe o saldo do jogador. O id="saldo" é o alvo do JavaScript para atualização em tempo real após cada aposta.

<form id="foguete-form">	Contêiner da Aposta	Agrupa os campos de submissão. O id="foguete-form" é crucial, pois o main.js o usa para identificar o jogo (foguete) e iniciar a chamada AJAX para a rota /play/foguete.
<input type="number" name="bet" ...>	Valor da Aposta	Campo para o usuário digitar quanto deseja apostar. O name="bet" é o nome da variável enviada ao servidor.
<select name="predict" id="predict">	Escolha do Palpite	Permite ao usuário selecionar sua previsão (o palpite). O name="predict" envia a escolha (up ou down) para o servidor.
<option value="up/down">	Opções	As duas únicas escolhas possíveis para o jogador, representando a aposta de 50/50.
<button type="submit">Lançar</button>	Ação	Botão que envia o formulário e aciona a lógica da rodada no main.js.

<div id="resultado">	Mensagens do Jogo	Área vazia onde o JavaScript injetará a mensagem final da rodada (ex: "Você ganhou!", o valor do payout e se o foguete subiu ou desceu).
<a>	Hyperlink	Link para o Lobby, permitindo ao jogador retornar à página de seleção de jogos.

###  GAME_ROULETTE.HTML

<title>Roleta</title>	Título	Define o título que aparece na aba do navegador.
<link rel="stylesheet" ...>	Estilo (CSS)	Liga o arquivo style.css para aplicar o estilo visual do site.
<script src="..." defer></script>	Comportamento (JS)	Inclui o main.js. Isso permite que a submissão do formulário seja interceptada e enviada ao servidor de forma assíncrona (AJAX).
<div class="container">	Layout	Contêiner principal para o conteúdo da página.

<h1>Roleta</h1>	Título Principal	O nome do jogo.
<p>Multiplicador: 3x (chance fixa de 15%)</p>	Regra do Jogo	Informa que, ao acertar a cor, o jogador ganha 3 vezes a aposta, mas a chance de acerto é fixa em 15% (conforme definido no app.py), independente da cor escolhida.
<span id="saldo">...</span>	Saldo Dinâmico	O id="saldo" é o ponto onde o JavaScript (main.js) atualiza o saldo do jogador na tela após o resultado da rodada.

<form id="roleta-form">	Contêiner da Aposta	Agrupa os campos de submissão. O id="roleta-form" é essencial, pois o main.js o identifica para chamar a rota /play/roleta no servidor.
<input type="number" name="bet" ...>	Valor da Aposta	O campo onde o usuário insere a aposta. O name="bet" é o nome da variável que o servidor espera para saber o valor da aposta.
<select name="pick" required>	Escolha da Cor	Permite ao usuário selecionar uma das opções de aposta. O name="pick" é o nome da variável enviada ao servidor, que contém o palpite do jogador (azul, vermelho ou preto).
<option value="cor">	Opções de Aposta	As cores disponíveis para apostar.
<button type="submit">Rodar</button>	Ação	Aciona a submissão do formulário e o início da rodada no backend, gerenciado pelo main.js.

<div id="resultado">	Mensagens do Jogo	A área onde o JavaScript injetará dinamicamente o resultado da rodada (ex: "Você perdeu", "Você ganhou na cor azul!", prêmio e novo saldo).
<a>	Hyperlink	Link para voltar ao Lobby.




###  `GAME_SLOTS.HTML` (Caça-Níqueis)

#### CODIGO HTML

<!DOCTYPE html> – Define o tipo do documento (HTML5).
<html lang="pt-br"> – Define idioma da página.
<head> – Contém metadados e links (CSS/JS).
<meta charset="UTF-8"> – Codificação de caracteres.
<title> – Título da aba do navegador.
<link rel="stylesheet"> – Importa o CSS.
<style> – Estilo específico da página.
<script src="..." defer> – Importa JS sem travar a renderização.
<body> – Conteúdo visível da página.
<div class="container">` – Caixa central de layout.
<h1> – Título principal.
<p> – Texto explicativo.
<form> – Formulário de aposta.
<input> – Campo de valor da aposta.
<button> – Botão de ação (Girar).
<div id="reels"> – Área dos rolos (símbolos).
<div id="resultado"> – Mostra o resultado da jogada.
<a href="{{ url_for('lobby') }}"> – Link para voltar ao Lobby.


###  `HISTORY.HTML` (Histórico de Partidas)

####  CODIGO HTML

<table> – Tabela para listar histórico.
<tr> – Linha da tabela.
<th> – Cabeçalho da tabela (Jogo, Aposta, etc.).
<td> – Dados de cada partida (dentro do loop `{% for partida in partidas %}`).

---

###  `LOBBY.HTML` (Lobby Principal)

* `{% extends 'base.html' %}` – Herda o layout base.
* `{% block content %}` – Início da área de conteúdo principal.

<div class="topbar"> – Barra superior com nome do usuário e saldo.
<div class="user-dropdown"> – Dropdown com botão de depósito e logout.
<nav> – Menu de navegação para jogos.
<a href="{{ url_for(...) }}"> – Links dinâmicos com Flask.
<div id="depositModal"> – Janela modal para depósito.
<form id="deposit-form"> – Formulário de envio de depósito.
<input type="number"> – Campo de valor.
<button> – Botão "Depositar".

---

### `LOGIN.HTML` (Tela de Login)
## CODIGO HTML

<form method="POST" action="/login"> – Formulário que envia nome via POST.
<input type="text" name="name"> – Campo de entrada do nome.
<button>` – Botão de envio ("Entrar").

---

### `STATIC/CSS/STYLE.CSS` (Estilo Visual)

#### Seletores e estilos:

* `body` – Fundo escuro, texto claro, centralização.
* `.container` – Caixa com sombra, margem e bordas arredondadas.
* `input, button` – Padding, cor, borda, radius.
* `button:hover` – Efeito hover (destaque amarelo).
* `table, th, td` – Layout e bordas das tabelas.
* `nav a, a` – Links estilizados (cor, fundo, hover).

---

###`STATIC/JS/MAIN.JS` (JavaScript do Frontend)

#### Funções:

* Captura de formulários de jogos (`form[id$='-form']`).
* Envio de apostas via `fetch()` para `/play/<game>`.
* Atualiza saldo e resultado dinamicamente.
* Controla:
    - Dropdown do usuário (abrir/fechar).
    - Modal de depósito (abrir/fechar).
    - Envio do formulário de depósito via AJAX.
* Atualiza `<span id="saldo">` na tela.
* Atualiza `<div id="resultado">` com mensagem da rodada.

---

 ##'APP.PY` (Principal – Backend do Cassino)

## Funções principais:

* `@app.route(...)` – Define as rotas do servidor.
* `request.form`, `request.get_json()` – Captura dados do frontend.
* `session` – Gerencia a sessão do jogador (login, saldo).
* `render_template()` – Renderiza templates HTML.
* `redirect(url_for(...))` – Redirecionamento de página.
* `jsonify(...)` – Resposta em JSON para o frontend (AJAX).
* Lógica completa dos jogos:
    - Campo Minado
    - Caça Níqueis
    - Dados
    - Roleta
    - Foguete
* Rota de histórico, depósito, logout etc.

---

`MODELS.PY` (Banco de Dados – ORM com SQLAlchemy)

####  Classes:

* **Player** (Usuário):
    - `id`, `name`, `balance`, `initial_deposit`, `created_at`
    - Relacionamento com `Match` (partidas).
* **Match** (Histórico de partidas):
    - `id`, `player_id`, `game`, `bet`, `payout`, `balance_after`, `played_at`

---

 `EXTENSIONS.PY` (Integração com Banco de Dados)

Código:

```python
from flask_sqlalchemy import SQLAlchemy
db = SQLAlchemy()
```

* Cria a instância do SQLAlchemy (`db`).
* Usado por `app.py` e `models.py` para acessar o banco de dados (`cassino.db`).

---


